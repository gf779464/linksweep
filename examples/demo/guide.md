# Guide

## Install

Install the MoonBit toolchain, then run LinkSweep from the repository root.

## Install

This duplicate heading intentionally creates the `install-1` anchor and tests
GitHub-style duplicate anchor numbering.
